services:
  gateway_service:
    build: ./gateway_service
    env_file: .env
    dns:
      - 1.1.1.1
      - 8.8.8.8
    volumes:
      - ./gateway_service:/app
      - ./media_sandbox:/tmp/kynto_media
    networks:
      - kynto_internal
    restart: unless-stopped
    healthcheck:
      # Slack SocketMode bot — no HTTP server; confirm process PID 1 is alive
      test: ["CMD-SHELL", "kill -0 1"]
      interval: 20s
      timeout: 5s
      retries: 3
      start_period: 15s

  openclaw:
    image: alpine/openclaw:latest
    env_file: .env
    volumes:
      - ./.openclaw:/root/.openclaw
      - ./agent_workspace:/root/openclaw/workspace
    networks:
      - kynto_internal
    depends_on:
      docker_mcp_bridge:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:18789/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  docker_mcp_bridge:
    build: ./docker_mcp_bridge
    volumes:
      - /run/user/1000/docker.sock:/var/run/docker.sock
      - ./logs/audit_trail.log:/var/log/kynto_audit.log
      - ./agent_workspace:/workspace
    environment:
      - ALLOWED_ACTIONS=list,logs,restart,start,stop
      - ENFORCE_APPEND_ONLY_LOGS=true
    networks:
      - kynto_internal
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/system/info"]
      interval: 20s
      timeout: 5s
      retries: 3
      start_period: 10s

networks:
  kynto_internal:
    driver: bridge
